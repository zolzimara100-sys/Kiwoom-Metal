# 🔒 보안 설정 파일 템플릿
# 이 파일을 복사해서 'application-secret.properties'로 저장하고 실제 값을 입력하세요

# Kiwoom API Credentials
kiwoom.api.app-key=YOUR_APP_KEY_HERE
kiwoom.api.app-secret=YOUR_APP_SECRET_HERE
kiwoom.api.account-number=YOUR_ACCOUNT_NUMBER_HERE

# 설정 방법:
# 1. 이 파일을 복사: cp application-secret.properties.template application-secret.properties
# 2. application-secret.properties 파일을 열고 실제 값으로 변경
# 3. Git에 커밋하지 마세요! (.gitignore에 이미 추가됨)
#
# 서버 실행:
# ./gradlew bootRun --args='--spring.profiles.active=dev,secret'
